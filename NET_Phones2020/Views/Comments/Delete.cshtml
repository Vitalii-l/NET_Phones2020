@model NET_Phones2020.Models.Comment
@using Microsoft.AspNet.Identity;

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

@if (Model.UserName == User.Identity.GetUserName() || User.Identity.GetUserName() == "admin")
{

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Comment</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.UserName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.UserName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Comments)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Comments)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.DatePublish)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.DatePublish)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Phone.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Phone.Name)
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to Phone", "Details", "Phones", new { id = Model.PhonesId}, null)
    </p>
    }
</div>
}
else {
    <h2> У вас нет прав удалять комментарий!</h2>
    <h3> Phone Title: "@Html.DisplayFor(model => model.Phone.Name)" </h3>
    @Html.ActionLink("Back to Phone", "Details", "Phones", new { id = ViewBag.PhoneId }, null)
}
